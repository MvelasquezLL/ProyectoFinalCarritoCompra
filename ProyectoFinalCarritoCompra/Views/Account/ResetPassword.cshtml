@model ProyectoFinalCarritoCompra.Models.ResetPasswordViewModel
@{
    ViewBag.Title = "Restablecer contraseña";
}

<main aria-labelledby="title">
    <div class="row justify-content-center pt-3">
        <div class="col-lg-6 col-sm-12">
            <div class="card" style="border-radius: 15px; background-color: #1d0b41; color: aliceblue;">
                <div class="card-body p-5">



                    @using (Html.BeginForm("ResetPassword", "Account", FormMethod.Post, new { role = "form" }))
                    {
                        @Html.AntiForgeryToken()

                        <h2 class="text-center" id="title">@ViewBag.Title.</h2>
                        <h4 class="text-center">Restablezca la contraseña.</h4>
                        <hr />
                        @Html.ValidationSummary("", new { @class = "text-danger" })
                        @Html.HiddenFor(model => model.Code)
                        <div class="row justify-content-center">
                            @Html.LabelFor(m => m.Email, new { @class = "col-md-8 col-form-label" })
                            <div class="col-md-10">
                                @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
                            </div>
                        </div>
                        <div class="row justify-content-center">
                            @Html.LabelFor(m => m.Password, new { @class = "col-md-8 col-form-label" })
                            <div class="col-md-10">
                                @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
                            </div>
                        </div>
                        <div class="row justify-content-center">
                            @Html.LabelFor(m => m.ConfirmPassword, new { @class = "col-md-8 col-form-label" })
                            <div class="col-md-10">
                                @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control" })
                            </div>
                        </div>
                        <div class="row mt-3">
                            <div class="offset-md-2 col-md-10">
                                <input type="submit" class="btn btn-outline-light" value="Restablecer" />
                            </div>
                        </div>
                    }

                </div>
            </div>
        </div>
    </div>
</main>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
